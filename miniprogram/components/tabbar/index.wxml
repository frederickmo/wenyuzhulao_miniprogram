<van-tabbar active="{{ active }}" bind:change="onChange">
  <van-tabbar-item style="font-size: 200rpx;" wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{
    item.text
  }}</van-tabbar-item>
</van-tabbar>
